class Student::CompetenciesController < ApplicationController
  # before_action { @competency = params[:competency_id] }
  # def show
  #   @learning_objectives = LearningObjective.where(user_id: current_user.id, competency_id: @competency)
  # end
end
