Dashboard Student
%table
  %tr
    %th Competentie
  - @competency.each do |c|
    %tr
      %td= link_to c.name, student_competency_learning_objectives_path(c)
= link_to 'Een nieuw leerdoel aanmaken', new_student_competency_learning_objective_path(@competency.first.id)

