Alle studenten
%table
  %tr
    %th Student naam
    %th Student email 
  = @student.count
  - @student.each do |st|
    %tr
      %td= st.education_name
      %td= st.email
      %td= @student.count


Alle docenten
%table
  %tr
    %th Docent naam
    %th Docent email 
  = @teacher.count
  - @teacher.each do |te|
    %tr
      %td= te.education_name
      %td= te.email

= link_to 'Terug naar dashboard', teacher_root_path